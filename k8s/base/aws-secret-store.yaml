apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: aws-secrets-store
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        secretRef:
          accessKeyIDSecretRef:
            name: aws-secrets-reader-creds
            key: access-key-id
          secretAccessKeySecretRef:
            name: aws-secrets-reader-creds
            key: secret-access-key
